window.addEventListener("load", event => {
    const lastVisited = null // localStorage.getItem("lastSearch")
    loadEls()
    // if (!lastVisited) pageRoot("Milton_Ramos").then(data => {
    //     // localStorage.setItem("lastSearch", JSON.stringify(data))
    //     console.log(data)
    //     loadVisual(data)
    // })
    // else loadVisual(lastVisited)
})